if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

L  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

!  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

D  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

v  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

U  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

P  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

A  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

B  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

A  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

E  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

E  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

R  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

V  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

I  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

C  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

E  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

R  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

O  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

L  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

E  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

K  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

E  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

Y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

!  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

/  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

/  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

R  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

R  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

<  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

k  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

>  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

k  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

|  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

|  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

k  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

k  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

A  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

H  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

U  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

k  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

N  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

|  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

|  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

0  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

0  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

/  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

/  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

H  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

|  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

|  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

`  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

[  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

H  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

$  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

$  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

_  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

]  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

`  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

`  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

$  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

\  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

$  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

`  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

=  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

I  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

R  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

J  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

O  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

N  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

m  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

5  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

0  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

0  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

H  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

C  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

T  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

/  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

j  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

w  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

R  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

J  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

S  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

O  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

N  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

g  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

f  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

k  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

b  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

(  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

?  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

u  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

2  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

0  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

0  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

h  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

{  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

.  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

H  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

d  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

r  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

C  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

-  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

T  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

y  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

e  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

:  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

p  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

l  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

c  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

a  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

t  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

i  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

/  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

j  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

s  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

o  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

n  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

"  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

,  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

   if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

}  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

)  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

;  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }


  if (target_kind === "field") {
    // field_audits a son propre schéma (workflow des missions terrain)
    table = "field_audits";
    const trace = payload.hub_mission_id || payload.hub_report_id
      ? `\n[Hub mission=${payload.hub_mission_id ?? "?"} report=${payload.hub_report_id ?? "?"}]` : "";
    const findings = payload.findings == null ? "" : String(payload.findings);
    row = {
      audit_type: payload.scope || "Audit logistique",
      target_type: "logistics_partner",
      target_name: payload.process || "Audit Hub mission",
      scheduled_date: date,
      completed_date: status === "completed" ? date : null,
      status,
      score: payload.score ?? null,
      findings: `${findings}${payload.notes ? `\nNotes (${auditor}) : ${payload.notes}` : ` (auditeur : ${auditor})`}${trace}`,
      recommendations: payload.recommendations == null ? null : `${payload.recommendations} recommandation(s)`,
      app_origin: "audit_hub",
      workflow_status: "mission", // la règle intranet la passe à "synced"
    };
  } else if (target_kind === "ops") {
    table = "ops_audits";
    row = {
      ...row,
      process: payload.process ?? "Audit Hub mission",
      scope: payload.scope ?? "",
      recommendations: payload.recommendations ?? null,
      findings: typeof payload.findings === "number" ? String(payload.findings) : payload.findings ?? null,
    };
  } else {
    table = "supplier_audits";
    row = {
      ...row,
      supplier: payload.supplier ?? "Unknown supplier",
      category: payload.category ?? "general",
      findings: typeof payload.findings === "string" ? Number(payload.findings) || 0 : payload.findings ?? 0,
    };
  }

